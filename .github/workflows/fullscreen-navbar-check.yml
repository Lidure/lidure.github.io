name: Fullscreen navbar check

on:
  push:
    branches: [fix/fullscreen-navbar-transparency]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: node --test tests/fullscreen-navbar-transparency.test.mjs tests/firefly-wallpaper-modes.test.mjs tests/visual-settings.test.mjs
      - run: npm run build
