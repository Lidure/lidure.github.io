name: Verify Firefly Transition Layer

on:
  push:
    branches:
      - fix/firefly-transition-content-layer

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Focused transition contract
        run: node --test tests/firefly-page-shell-transition.test.mjs
      - name: Full test suite
        run: npm test
      - name: Build
        run: npm run build
