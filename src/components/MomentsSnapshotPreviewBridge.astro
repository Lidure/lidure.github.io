<!-- SNAPSHOTS clicks are handled directly by MomentsPinControls.astro.
     Keep this compatibility component mounted for now, but do not intercept
     document click events; capture-phase interception made thumbnail clicks
     fragile across Astro page transitions. -->
